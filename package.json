{
  "name": "adrian-learns",
  "version": "0.0.1",
  "main": "index.js",
  "private": "true",
  "scripts": {
    "test": "npm run test --workspaces",
    "build": "tsc -b tsconfig.build.json",
    "dev": "tsc -b -w tsconfig.build.json"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "ts-twitch-irc": "^0.2.0"
  }
}
